// Yahoo Finance helper removed; provide a stub so prefetch compiles

export async function fetchYahooFin(_symbol: string, _range: string, _interval: string): Promise<any> {
  return { ok: false };
}


