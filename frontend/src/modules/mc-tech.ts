export function initMcTech(){
  // Placeholder heavy module (Moneycontrol technicals)
  if ((window as any)._mcTechInit) return;
  (window as any)._mcTechInit = true;
  // Future: render technical charts when symbol changes.
}
